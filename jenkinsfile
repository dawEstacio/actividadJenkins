pipeline {
    agent any
    tools {nodejs "miNodejs"}
    parameters {
        string(name: 'persona_a_saludar', description: 'persona que quiero saludar')
    }
    stages {
        stage('Execution') {
            steps {
                sh 'node src/index.js "${persona_a_saludar}"'
            }
        }
       
    }
}
