pipeline {
    agent any
    tools {nodejs "miNodejs"}

    environment {
        bot_token = credentials('token_telegram')
        chat_token = credentials('chatId')
    }

    stages {
        stage('Execution') {
            steps {
            
                echo "hola"
            }

        }
       
    }
}
