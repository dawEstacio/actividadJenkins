pipeline {
    agent any
    tools {nodejs "miNodejs"}
    stages {
        stage('Install') {
            steps {
                sh 'npm install' 
            }
        }
        stage('Lint') {
            steps {
                sh 'npm run lint'
            }
        }
    }
}
